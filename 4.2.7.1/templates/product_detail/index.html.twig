{% extends 'base.html.twig' %}

{% block title %}{{product.Name}}{% endblock %}

{% block body %}

    <div class="card mb-3">
        <div class="row no-gutters">
            <div class="col-md-4">
                <img src="{{ product.Image }}" class="card-img" alt="...">
            </div>
            <div class="col-md-8">
                <div class="card-body">
                    <h3 class="card-title">{{product.Name}}</h3>
                    <p class="card-text"> {{product.Price}} Kč</p>
                    <p class="card-text"> {{product.Description}} Kč</p>
                </div>
            </div>
        </div>
    </div>





    <a href="{{ path('product_list') }}">Zpět k produktům</a>



{% endblock %}
